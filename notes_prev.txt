I NEED TO START OUT WRITING INSTRUCTIONS FOR THE SITE SUCH as
First: set the menu items using template names with orders between 100 - 999.  The template names become top-level menu titles.
Then, each template has a dynamic folder. This becomes the basis of the submenu titles.
The templates themselsves will be the holders of the page content.  The pagebuilder options will land on the submenu pages for the listing pages, and the template for the content pages.

When it comes to individual (content) pages, I need a layout (Pb).  If I use "about.php" as an example, I cannot use any pagebuilder on the root page because it's a clonable page. (This has since been solved - PB is not available as cloned pages. "Manage Globals" now has the homeview listing page - respective to its menu title - PB and the content page PB on it ) The clonable part gives it the dynamic folders.  W/out the dynamic folders, I cannot use the folder structure. 



One alternative is to create a clonable page called Employees with the dynamic folder structure and use "about" as a non-clonable page.  
    - Navigation - for About, will need k_template_name similar to nica site.
    - emp.php should be in a subfolder called content titled "Employees"
    - change '/menu/' to '/submenu/' and change 'abouts.php' to 'about_emp.php' (similar to relations naming convention)
    - about.php (in root) will have pb for the content page layout only and will import (embed) in all content
Root menu order# = 1xxx parent='_site_', bmenu = 2xxx (same xxx) parent=(none), clone = 3xxx (same xxx) parent='_clone_'

If using the templates (and template order#) for nav titles, how to connect the root with the dropdowns created by the user?
Can the menu be pages from a nestable template with a masquarade to the clone/xxxx.php pages? A: only if the nested template is Index.php. 
All nestable pages created from the main nestable Index (which could have the combonation home or page pagebuilders on them????) can be the main menu structure, then all the sub pages off them can  masq or point to what was the dynamic folders template....???? 

  If nested, how do submenu items get created? parent nested-pages would be the sub-menu pages
Maybe each ROOT page (about, services, products, portfolio, etc) can be it's own nested page group and the navbar can still use the order# to enumerate the titles?
Nested seems to be working, but I'm not sure how I'm going to list all the content... employees, services, products, individual portfolios, individual blogs..... how do these tie into the menu?
This may not work simply due to the lack of styling the navbar.


NEW IDEA:the question is how to get single content onto the clonable template. The clonable template for about is the employee.... pb makes each cloned page a built page. This will nawt doo. call the schoolmaster!


A COMPLETE NIGHTMARE!!!

I cannot think of a way to use templates of created pages to create a menu with dropdown folders for the others. Even if I do hard-coded top-level, the sub-menus will have to navigate.

If using dynamic folders which is perfect for building all the submenu pages, I cannot build the single post page from this.  The pb design cannot be built somewhere else and embedded in. 


Single Content pages from dynamic menus won't show up on the nav menu, only from submenu pages..... so.....what if the page links (that show up from the submenu pages) land on the clone/xxx pages that pull the single cloned content from the root clonable..... 
-so make the content for the submenu with page links with fomatted url
-page link uses url to pull correct content from correct page ( I don't see how that will lay out dynamically) all page vars will need to be cms:set to page vars.. per layout.

I changed the folder name clone to content/xxx
If the the page links will have folder ids, etc in their dump, so I should be able to pull them that way in the layout. 
Just make Left Admin Menu items for the Single Page Mods from the content/xxxx.php
so the link in the list page will be build from the cms:pages itteration.  set tplt="content/<cms:show k_template_name />.  then the link is <cms:link masterpage=tplt ?cat=k_page_folderid&id=k_page_id

then extract those into vars.  Each Top-Level Menu will have it's respective single-content pb item..... but where on the left side?  small matter.

First thing, right now, I need to see the content get displayed into a single post from an "if k_is_page", setting the vars from the page, and landing on content/xxx page, and extracting the vars.
This works, but I have to pull template name, folder name and page id into the url... unless I can figure out how to strip "content" off the template name to use... which I'm sure anton could tell me.


There's no way to link the single page to the listing pb-built page.  PB already builds itself in an iframe and are pre-made. No way to put the single vars in them when stocking up.


So, in order for the pb tile to build, it has to know what it's building.  It's pre-built, it's not dynamically built.  It doesn't build as the page loads, it is already built before the page loads.

The menu item PBs need to be set up with the pages tags already and build from them.  Can I get the "Add new" button to point to another template that actually creates the cloned page?

dynamic folders list as folders of a clonable page.  If dynamic folders could work on non-clonable page, then the menu would list, the pb single content could be built, 

I think each pb module (that is a listing mod) is going to have to be set as it's own listing page.... only way I can think of that rn is a dropdown of the options or hard-code for each menu item.
what about submenus with submenus?  What about submenus with submenus and pages?  
What about toplevel menus with only pages and no submenu items?


OK, so what about hardcoding the top-level menu and creating clonable templates that are tied to the top-level nav menu (via <cms:pages on the)

OK so, now I'm seeing that cms:pages CAN import PB data from a non-clonable template... .this changes everthing.......

I can import a single non-clonable pb into a clonable tpl using cms:pages masterpage(that single tpl w/pb in it).  BUT, how do I get the menu pbs to list the proper lists from that main template?
The PB tiles must have the vars already set for main page template and pages (or folders) INSIDE each tile being built!! how can that happen when building a tile insde the admin for stock???

One of the ways is to hard-code the top-level template (at least) into each listing PBmod built inside a dynamic folder.... 
another way is to have dropdowns of each folder level.
If the user is choosing a dynamic folder of About.php, to build out, they won't want to also have to use dropdowns to choose what folder the page belongs to. If that's what they have to do, what's the point of being in the About pages section anyway.....
How do I pull the current template and folder into the pb tile so it's already there?  inside the tile put the abort/dump to see what it sees.


So this is it... it cannot happen in this current mindset. There is no way to logically set each module for a proposed site without hardcoding the mods...
The only break would be to seperate all 'list-page' mods vs random link pages.  
Then all 'list-page' mods will have dropdown boxes to choose where they go, 
and randoms will have all menu items from which to list.
All top-level menu items will need to be hard-coded and pages can only go 1-deep from those.  


the way NICA does it, for each mod there is a dropdown as to what menu item the mod should be representing.  Then there are variables built around each to build the vars that will fulfill that display.
Even in larger sites, it should be title, image, excerpt as the set major options. But there is also page link


I think for a large site, I will need to produce a side Admin Menu of each hard-coded top-level menu (TLM) item and in each of those have a menu list (clonable page) and single input page that has a dropdown of the clonable page menu list for choosing which folder this page belongs to. 
What if the user wants 2 levels of menu items? On the page that declares the folders, I can have a dropdown that shows all the pages and ask if this current page being build is to be a submenu of one of the pags in the dropdown.  IF it is then there can be a second-level dropdown.  But that dropdown can only show direct pages, so a 2nd level page cannot ever be chosen.


Step 1:Edit kfunctions to show the NavBar Menu Titles for the pages created in steps 2,3.  High order numbers for lower placments - Can't really do this since the pages are dynamically built. 

Step 2: "Top Level Menu" Create a clonable template with the ONLY editable field (config_form_view) being the "Menu Title" and PB sections. These PB sections will be hard-coded for the TLM title and list all subm pages created from their related pages.  This will allow the PB mod 'subject matter' to be chosen.  For this template, the  config_list_view will be sortable.  Make the pub/unpub be a seen radio button called visible on menu. These pages cannot be created or deleted, but can be renamed and or hidden. The pages will be pre-created and the only option is to rename or hide from the menu. Sortable to change top-level menu placment.

Step 3: Create a cloneable page called "Sub-menu creator" RELATED ITS RESPECTIVE TLM (so all created pages can be listed for 2nd lvl subm choices) The ONLY editable field (config_form_view)will be the "sub-menu title" field and publish/unpub for visibility. PB mods can be used for sub-menu images, desc, etc.   


EACH OF THESE CREATED SUBM PAGES WILL HAVE THEIR OWN PB MODS.....   Each clonable submenu page will have a PB layout on it that will ultimately be hardcoded with the top-level menu items.... because of this, each pb mod will have dropdown choices of what "subject matter" each mod will be- which means, which TLM page or subm page will this particular mod belong.. 

Step 3: Create the navbar menu that pulls the top level visible (and order) then the IF statments that pull the submenu pages below each title.



So these steps won't really work by themselves.  There's no easy way to connect St2 with St1 - there's relations, but that becomes combersome and confusing with each choice. Plus, as pages instead of templates or folders, there isn't an automatic .php in the name for page recognition when it comes to 'templates' or folders.... 

Looking to see if routes may be able to help.  In the end, the dynamic folders may be the better option using routes... looking into it.
I'll use services.php as the routes template.

Anton showed me how to put pagebuilder into the template globals. Upon initial use it worked. So, I'm going back to using the About page how I had it before and working to see if the globals will solve the problem of single-content display. 

SO I'M BACK TO THE BEGINNING OF WORKING WITH A HARD-CODED TOP-LEVEL MENU, using Manage Folders for the sub-menu, using Manage Globals for the single-Content, using IF statements to seperate views, and somehow limiting the user to only 2 levels of menu dropdowns (to be figured out later - possibly only showing top layer of choices in Parent Folder option for Manage Folders/Submenu). I will not be using custom routes.


My current issue is, since each cloned page in root.php template won't have pagebuilder in it, but only the editables that will be the single content vars, how does the layout for the Home view get planned?  My initial thought is THIS is what can be imported from another template (currently clone/xxxx.php, but may need to change to homeview/xxx.php or something). Can str replace be used in the tiles to know what their parent is (or whatever holds the homeview/xxx.php data, then use that to search the current template?)  If not, importing a PB for home view may lead me to the same obstical. Ofc a home view also can have other parts of the site in it as well. PB mods can be made specifically for other root folders.

I'm not sure hard-coding of things is such a bad idea.  I can't assume the end-user to build editables in any created page. I'll have to hard-code things like, service desc, prod variables, price, and other oddities that may come.  Menu items, nutritional info....



Done:  Make the homeview page layout and import to homepage

DONE:  create all employees, services, several products.
DONE:  create the fields that will be common to all screens for universal use on PB mods.  For easier use, the names of the editables can not be individualized and need to be common as well. 

DONE: (expt Contact) Decide which mods will be used for single-content layout that is wrapped by PB on the content page.  Then, take mark them so they can be recognized and used only very judiciously thruout site. 

There shouldn't need to be a homeview separation, right?  Just because stuff is in GLOBALS doesn't mean it has to be called.  Plus, there can be 2 Globals, just like what Trendo said in his chat....
DONE: Homeview/ deleted. FIX All main page editables to be universal - no repetitive in editable names - if all of them have it, none of them need it and merge the homeview into the homepage globals
DONE: Individual Mods to their own public page (groups ok) for linked example of how a finished mod can look - Can be one single html page with anchors, heading (needs to be how the tile button is titled) seperating each;    
 Decide grouping of PB mods, how to catagoize them
The grouping will require decision-making::: 
banner, 
list, 
manual, 
gallery, 
compound, 
form (cannot pre-build content layout)  Current blocks to dissolve:: mods, frame, content, bs-mods

OMG - it looks like I just figued out a way to name the tiles as they are listed as buttons on the Admin page w/out that title showing on the main site. using dump all on the pb_wrapper (under misc), I noticed that k_template__tile_label was the var that held the tile label.  So I added cms:show for that var on the pb_misc (<hr><br><h4><cms:show k_template__tile_label /></h4>).  I can't remember if I hard-coded that to only show in the back-end but I know that is possible to do. As of now, that title doesn't appear on the main site. 


The only way to TITLE the page where the Tile buttons are is to make it it's own page.  A Mosaic lable cannot be shown anywhere on the Admin page to differentiate between the different level of tile buttons.  

So now the question is, can I title the Admin Side panel is sections where the titles above "Banner, List...." etc can be Section Headings and the PB builder pages can be filed under them?

I will assume that I should be able to, and act like I can while diffentiating the sections using kfunctions for now.

DONE: Adjust k_functions sections for above titling. Start to rebuild the PB pages in their correct categories.



NEXTEP: Continue to build the PB mods to completion.. UNIVERSAL USABILY MINDSET FROM THE BEGINNING - Do not add and PB editing YET.  First work on all editables. On-page editing to come next.

The head argument was if I should separate pages by user complexity. ie should "Transitions" be seperate pages of "Transitions - Simple", "Transitions - Complex".  IF they are seperate pages or even sepeate mosaics, then when it comes to the site page building with pagebuilder tags, each one of those tiles equates to a page or mosaic. This would increase not only the total amount of choosable buttons for the user, but it could potentially dictate the page segmentation. It could take away the choices if using seperate pb tags in the site page template. SOLUTION: go ahead and pull all subject mattered tiles in one mosaic and later, we'll see if there is a way to format the buttons, line breaks, titleing, etc. 


NEW IDEA - reusable .htm pages.  Such as background image/color is used on all transitions. No need to re-write that code for each tile. Each tile can just use that .htm page.

as a theory, since these are transitions and not image galleries, the transitions as transitions should only use background images, not img elements.  Otherwise, use one of the galleries for that section.


Remake the transitions to have a type of questionaire at the first to say something like:
"I want this transition section to have:" then checkboxes/dropdowns of bkgd/clr, text, button(s), video element, etc.... then the sections pop up as per what's chosen

ACTUALLY:  Make the options it's own module to add to each tile. It should contain all the functions, which means all groups will have to use the same names. 





    STEP1: find and name all vars (editables) necessary for that specific block (.htm)
    STEP2: Create the IF tree to set all editables for universal use - this must include any and all vars for pertinent menu items (contact won't need a portfilio list), including blanks.



HINT if needed heres forum on setting links using custom fields https://www.couchcms.com/forum/viewtopic.php?f=4&t=13712
    

THEN: read about admin page customization again to not build too far w/out formatting admin side
________________________________________________
I'd left this site for a while - about 6 months since the last update above.  Admin customazation didn't yeild the results I wanted, and then holidays, and I moved. 
Getting back to this now, my directive is:
make sure all element names are very generic - it will help with the sharing of code
keep the hard-coded top-level menu - just allow them to be made invisible or hidden. Of course if hidden should they be able to be chosen as a dropdown? I want them to be able to be chosen, but somehow show they are hidden. Like strikeout font or something.

WORKING ON: create the ability to hide/show the top-level menu items and give the dropdowns for them the ability to show when they are hidden. 
WORKING ON: also give the menu item the ability to be renamed.  This name will also be what needs to show on the dropdown choice.











_____________________________________________

Naming convention for this site [(posh ecommerce = pec)]_[block or page]_[section def1]_[sec def2]_[elem def1]_[elem def2]_[editable]:: I've decided this is too much. Keep names small and general, no repeating.


Site nav and footer choices only to be edited in GLOBAL settings

Not going to be able to give the business changeable top-level menu items. But each page can have folders for sub-menu items. Top-level will be pre-determined.

To tie the final page to a menu item (top level is template) - use related pages and make relation go to a template.... or make 'content' pages for each menu item similar to nica.
This is the nav url built to be parsed for the cloned page
<li>
<a href="<cms:link masterpage='music.php'/>?page=<cms:show k_template_name />&post=<cms:show k_page_id />#pg_cntnt" >
<cms:show k_page_title />
</a>
</li>

These parse the url and set vars, using if the vars are set to direct which view is showing. 
<cms:set nca_msc_pg="<cms:gpc 'page' />" "global" />
<cms:set nca_msc_pg_id="<cms:gpc 'post' />" "global" />
<cms:if nca_msc_pg='content/track.php' || nca_msc_pg='content/album.php'>
    <cms:set nca_msc_redirect_link = "<cms:add_querystring "<cms:link masterpage=k_template_name />" "page=<cms:show nca_msc_pg />&post=<cms:show nca_msc_pg_id />#pg_cntnt" />" />
<cms:else />
    <cms:set nca_msc_redirect_link="<cms:show k_template_link />" />
</cms:if>



this isn't working with multi level because I can't think of any way to get the 2nd level, or the reverse related pages with folders to work (content/_____) with a nav bar that scrolls thru the templates.  
And since I cannot build folders from the front-end, let me try working with dynamic folders and views with a top-level that is preset.




SO, I WILL HARD CODE THE TOP-LEVEL MENU, BUT FOR EACH, I WILL MAKE FOLDERS THAT WILL SET UP MULTIPLE LEVELS OF MENUS.   BEFORE I DO THIS, I WILL NEED TO CREATE FINAL CONTENT PAGES FOR EACH CATEGORY AND SUBCATEGORY - WHICH WILL BE A LOT, BUT IT NEEDS TO BE THE NEXT STEP.




Dropdowns don't work because there's no way to tie the assigned dropdown with the page to list only one time on the menu.  It would take too much coding for the backwards reading: no way to relate... hhhmmm relate? 1 to many?..... 

Going with the Top Level Menu in the k_function headers - can dynamic folers be pagebuilders?
Put each Top-Level Menu Item in kfuc - with a single clonable dynamic folder, with the dynamic folder being pb


Top Level Menu Items - Prebuilt in all sites: 10-100


Home/About/Catelog/Portfolio/Blog/Contact 
    -Make each menu item a heading in the Admin Panel (using kfunctions)??  OR should each menu item be Nested Pages?
When creating sub-menu (non-content) pages, contextual dropdowns before the page can be saved:
    First dd is the top-level menu items (required)
    For each module that will have links, all content pages need to be categorized and available for the link
        -this site
        -external link
When creating a CONTENT PAGE, there will be contextual dropdowns:
    First dd is top-level menu (required)
    2nd dd will be the folders/subfolders for that top-level
    A FOLDER MUST BE CHOSEN to save the page
    
    
MAYBE for the colors issue, can I 'get' the color from the template (or wtvr) before pb grabs it, then put that var into the rendered page? 



make each top-level menu item it's own k-func heading But homepage will be on site along with globals - maybe higher auth for that(?).  
Each heading will have a cloneable submenu page which will become sub-menu titles/links and each page will have a dropdown option for all the other pages in case it is sub'd off of that. (listen in main menu using pages tag with whatever's chosen as the masterpage)
Each heading will hve a clonable content page with 2 contectual dropdowns.
    DD#1 - top-level menu titles (required to choose)




OR

kfunc headings of top level menu items with a clonable pb template - each cloned page title is the top-level menu item
next kfunc heading is sub-level menu ites with dd of the top-level pages to choose which it goes under





Every page, every different element will need its own number. A numbering convention is needed for each
Hero: 100-2999
thinbar: 3000-3999
site menu: 4000-4999
transitions: 5000-5999

SWIPER OPTIONS WILL ALL START IN THE 2K RANGE AND FOLLOW this pattern:
HEADER FORMAT SETTINGS (100 - 175 )
shades 100
image or video option 105


Textbox area size
slider delay time

paralax scroll effect rate
overylay 
Add video modal (could I also pop up a gallery?)



HEIGHT OF HEADER VH (needs to be early choice)	
height

HEIGHT OF HEADER padding (needs to be early choice)	
height

HEADER BG IMAGE SETTINGS (175-199)
main image
alt desc
clipped image
repeatable section 
repeatable image
alt desc


HEADER BG VIDEO SETTINGS (200-299)
Optional Row (to show with conditional function - should be a group)
Video source option
mp4 upload + rpt
webm upload + rpt
ogv upload + rpt
yt stream ID + rpt
yt stream start + rpt
yt stream stop + rpt
Vimeo stream ID + rpt
Vimeo stream start + rpt
Vimeo stream stop + rpt
repeatable video section



HEADER TEXT BLOCK SETTINGS (300 - 374 )
text alignment
textbox vertical alignment
textbox horizontal alignment
Textbox option (text, image, form)
text lines repeatable
text line
text color
heading size
text size
font weight
letter spacing
font style
font family

image (for image choice in textblock) (375 - 390)


text background option (390 - 419)
textbox padding (helps with background)
text bg color
text bg border
text bg border color

HEADER CALL TO ACTION (420 - 499)
option for no c2a
row to show or hide, should be group
C2a (submit btn) text + rpt
C2a button or link option  + rpt
C2a btn size + rpt
C2a btn width + rpt
C2a btn solid or outline choice + rpt
C2a btn color + rpt
C2a link color - shades of gray + rpt
c2a link dropdown + rpt
c2a link custom input + rpt
c2a multiple option
c2a padding




HEADER TEXT BLOCK FORM (option for textblock) (500 - 699)
form row (to show or hide - should be group)
form title line
form title text color
form title heading size
form title text size
form title font weight
form title letter spacing
form title font style
form title font family
form paragraph line
form paragraph text color
form paragraph heading size
form paragraph text size
form paragraph font weight
form paragraph letter spacing
form paragraph font style
form paragraph font family
form input repeatable
form input label line
form input label line text color
form input label line heading size
form input label line text size
form input label line font weight
form input label line letter spacing
form input label line font style
form input label line font family
form input required checkbox
required value params
required value param values
input error message
Form C2a (submit btn) text
Form C2a button or link option 
Form C2a btn size
Form C2a btn width
Form C2a btn solid or outline choice
Form C2a btn color
Form C2a link color - shades of gray



HEADER ADDING VIDEO MODA OPTION (700- 774)
row - to show or hide conditionally - should be group
vid-add source option
vid-add mp4 upload 
vid-add webm upload
vid-add ogv upload 
vid-add yt stream ID 
vid-add yt stream start
vid-add yt stream stop 
vid-add Vimeo stream ID 
vid-add Vimeo stream start
vid-add Vimeo stream stop 
vid-add play button shade
vid-add play button padding (space)



HEADER SOCIAL BUTTONS ADD (775- 799)
option to show
row to show or hide, should be group
icon only or btn
social icon
social btn size
social btn link
social btn color (if not branding)


BOTTOM RAIL option for or any header maybe a note that says not recommended with scrolling effect (message pops up if both scrolling effect and option turned on) (800-899)
option
row to show/hide if chosen
row size
row shade
text line
text color
text size
font weight
letter spacing
font style
font family


HDR COLORS (900-999)



SPECIALTY SETTINGS FOR SPECIFIC HEADERS (1000 - ??)
50/50 checkbox to swap sides (possible this being inside only a container background, the background could show image if wanted to, or color. will require recoding to get a background image to appear and also the 50/50 internal (div shadow wouldn't hurt)



OFFSET IMAGE -specialty setup for offset overlay (disappears on small screen) this is a completely special header, will need to look into it further to see about portability


CUSTOM COLORS (1200-1399)
CUSTOM FONTS (1400-1599)





	
	

	
